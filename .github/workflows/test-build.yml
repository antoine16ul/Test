name: build-test

on: push

jobs:
  
  linux-build:
    name: ubutu ${{ matrix.debug }}
    runs-on: ubuntu-18.04
    strategy:
      fail-fast: false
      matrix:
        debug: [ "Debug","Release" ]
    steps:
      - uses: actions/checkout@v2
      - name: build
        run: |
          mkdir -p build
          cd build
          cmake -DCMAKE_BUILD_TYPE=${{ matrix.debug }} ..
          make -j2
          ./test

      - uses: actions/upload-artifact@v2
        with:
          name: ubuntu-${{ matrix.debug }}
          path: build/test

  docker-build:
    name: Docker ${{ matrix.conf.name }} ${{ matrix.debug }}
    runs-on: ubuntu-latest
    container: ${{ matrix.conf.cont }}
    strategy:
      fail-fast: false
      matrix:
        debug: [ "Debug","Release" ]
        conf:
          #- { cont: "debian:10", name: "deb10"}
          #- { cont: "debian/buildd", name: "deb"}
          #- { cont: "debian:9", name: "deb9"}
          #- { cont: "centos:8", name: "cos8"}
          #- { cont: "centos:7", name: "cos7"}

    steps:
      - uses: actions/checkout@v2
      - name: build
        run: |
          apt-get update
          apt-get -y install cmake
          g++ --version
          cmake --version
          mkdir -p build
          cd build
          cmake -DCMAKE_BUILD_TYPE=${{ matrix.debug }} ..
          make -j2
          ./test

      - uses: actions/upload-artifact@v2
        with:
          name: Docker-${{ matrix.conf.name }}-${{ matrix.debug }}
          path: build/test

